name: WP007 Migration Bootstrap

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  parked:
    runs-on: ubuntu-latest
    steps:
      - run: echo "WP007 bootstrap parked while permanent corrections are committed."
